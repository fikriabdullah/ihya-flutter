import 'package:flutter/material.dart';

class dashboardMurid extends StatelessWidget {
  const dashboardMurid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
